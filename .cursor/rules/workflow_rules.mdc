---
description: 
globs: 
alwaysApply: true
---
## 📍 Инструкция для CURSOR AI: Пошаговая разработка как Senior Developer

### 🌟 Цель
Создать высококачественный, масштабируемый и безопасный продакшн-бот для криптовалютного арбитража. Работать поэтапно, писать код на уровне **senior-разработчика**, полностью соблюдая стандарты, лучшие практики и требования проекта.

---

### 🧠 Общие принципы

1. **Senior-уровень реализации**:
   - Код должен быть чистым, читаемым, масштабируемым.
   - Используй принципы SOLID, DRY, KISS.
   - Предпочитай модульную архитектуру и разделение ответственности (SRP).
   - Строго следуй pythonic стилю кода (PEP8 + best practices).

2. **Документация и читабельность**:
   - Все классы и функции должны содержать `docstrings` в стиле Google или NumPy.
   - Используй **type hints** (`Optional`, `List`, `Dict`, `Union`, `TypedDict`, `Protocol` и др.).
   - Если код требует пояснения — добавь комментарий или вынеси логику в отдельную функцию.

3. **Безопасность и конфиденциальность**:
   - Не храни пароли и ключи в коде.
   - Всегда используй `.env` и систему конфигурации.
   - Защищай данные и API ключи как на этапе хранения, так и при передаче.

4. **Логирование и контроль**:
   - Все важные действия должны логироваться (с уровнями: DEBUG, INFO, WARNING, ERROR, CRITICAL).
   - Логи должны быть читаемыми и пригодными для отладки в продакшне.

5. **Тесты и симуляции**:
   - Добавляй тесты к ключевым модулям и алгоритмам.
   - Для рискованных действий (например, торговля) сначала создавай безопасную симуляцию.

6. **Асинхронность и производительность**:
   - Используй `asyncio`, `aiohttp`, `asyncpg`, `asyncio.Queue` и др. там, где применимо.
   - Не блокируй event loop — избегай `time.sleep`, `requests` и др. синхронных вызовов.

---

### 📌 Правила выполнения задач

1. **Двигайся строго по порядку**, начиная с Этапа 1, Задача 1.1.
2. После **каждой задачи** (например, `1.1`, `1.2`):
   - Пометь задачу как выполненную (`[x]`)
   - Выведи сообщение:
     ```
     ✅ Задача {номер} завершена. Готов к следующей. Напишите: "продолжить".
     ```
   - Приостанови выполнение и **жди команды `продолжить`**.
3. После завершения всех подпунктов блока (например, всего раздела 3. Подключение к биржам):
   - Проверь интеграцию модуля с другими частями проекта.
   - Сделай логическую ревизию (код, логика, безопасность).
   - Напиши:
     ```
     ✅ Раздел {номер} завершён. Готов перейти к следующему. Жду "продолжить".
     ```
4. **Никогда не переходи к следующей задаче или этапу без разрешения.**

---

### 📦 Итоговая комплектация проекта (продакшн-уровень)

К финалу проекта ты должен:

- [ ] Объединить все модули в единый, работающий и устойчивый сервис
- [ ] Убедиться, что все конфигурации оформлены через `.env` и `config.py`
- [ ] Обеспечить стабильную работу на `Docker + docker-compose`
- [ ] Подключить `CI/CD` (если возможно, через GitHub Actions)
- [ ] Добавить README.md с полным описанием:
  - Установка и запуск
  - Архитектура
  - Примеры использования
  - Безопасность
  - Список зависимостей
- [ ] Финальная документация по каждому модулю
- [ ] Приложить файл `CHANGELOG.md` с описанием версий и изменений
- [ ] Провести полное покрытие тестами критических частей

---

### 🤖 Если задача неполная или требует уточнений:
- **Не додумывай** самостоятельно — задай вопрос.
- При возникновении неоднозначности, выведи:
  ```
  ⚠️ Задача {номер} требует уточнений. Опиши, что именно нужно сделать или подтвердить.
  ```

